import state from "@/storage/recipies/state";
import getters from "@/storage/recipies/getters";
import actions from "@/storage/recipies/actions";
import mutations from "@/storage/recipies/mutations";

export default {
    state: {...state},
    getters: {...getters},
    actions: {...actions},
    mutations: {...mutations}
}